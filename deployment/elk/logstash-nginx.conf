input {
  tcp {
    port => 5000
    codec => json_lines {
      target => "logstash_data"
    }
  }
}



output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "backend-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}